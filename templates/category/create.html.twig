{% extends 'base.html.twig' %}

{% block title %}Création d'une categorie{% endblock %}

{% block body %}
    <h1>Créez une catégorie</h1>

    {{form_start(formView)}}

    {{form_widget(formView)}}

    <button type="submit" class="btn btn-primary">
        <i class="fas fa-save">
            Créer la catégorie
        </i>
    </button>

    {{form_end(formView)}}

{% endblock %}
