<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Salut</title>
</head>
<body>
    <h1>Hello {{name}} ! Twig</h1>
    {# Exercice boucle for et if #}
    <ul>
    {% for age in ages %}
        <li>
        {% if age >= 18 %}
            {{'Adulte'}} 
        {% else %}
            {{'Enfant'}} 
        </li>
        {% endif %}
    {% endfor %}
    </ul>
    {# Correction légèrement différente #}
    <ul>
    {% for agge in ages %}
        {% if agge >= 18 %}
            <li>Adulte</li>
        {% else %}
            <li>Enfant</li>
        {% endif %}
    {% endfor %}
    </ul>
</body>
</html>