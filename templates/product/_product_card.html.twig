<div class="card mb-3" style="background-color: #DCDCDC; height:95%;">
    <img src="{{ p.picture }}" class="img-fluid" alt="Image produit">
        <div class="card-body">
            <h4 class="card-title">
                {{ p.name }};
            </h4>
            <h5 class="card-title">
            <strong>{{p.price / 100}} &euro;</strong>
            </h5>
            <span class="badge badge-info">
                {{p.category.name}}
            </span>
            <p class="card-text text-justify">
                {{ p.shortDescription }}
            </p>
        </div>
        <div class="mb-3 d-flex justify-content-center">
            <div>
                <a href="{{path('product_show', {'category_slug': p.category.slug, 'slug' : p.slug})}}" class="btn btn-primary btn-md">Détails</a>
            </div>
            <div>
                <a href="{{ path('cart_add', {'id': p.id}) }}" class="btn ml-3 btn-success btn-md">Ajouter</a>
            </div>
        </div>
</div>